// The physics engine. Deliberately has no dependencies except the test
// framework, which comes from the root build script. Everything here is plain
// Java maths that we can test quickly and read easily.
